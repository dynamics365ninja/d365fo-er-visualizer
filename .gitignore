node_modules/
dist/
out/
output/
generators/
.vite/
*.tsbuildinfo
.DS_Store
Thumbs.db

# Next.js (packages/site)
.next/
next-env.d.ts
# The SPA build staged into the site's public dir at deploy time
packages/site/public/app/

# Integration test — connection secrets (never commit)
scripts/.fno-integration.json

# Local-only ER XML samples used for manual UI testing
packages/ui/public/dev-samples/

# Machine-local Claude Code settings (permission allowlist etc.)
.claude/settings.local.json
